# 更新日志

所有对本项目的显著变更都会记录在此文件中。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [0.2.0] - 2026-09-08

### 新增

- 侧边栏无项目时展示空状态引导（添加项目 / 保存当前文件夹 / 新手引导）
- 新增「搜索并打开项目」命令，按名称 / 路径 / 文件夹模糊过滤后快速打开
- 项目树展示失效路径提示（警告图标 +「路径不存在」）与悬浮详情（完整路径 + 所属文件夹）
- 重新设计新手引导：移除截图，改为纯文字 + 步骤圆点指示器，自动适配浅色/深色

### 修复

- Webview 补充 Content-Security-Policy，抽离内联脚本与样式
- 统一 `showProjectPath` 默认值，消除配置声明与代码实现的不一致
- 修正路径比较，Windows 下忽略盘符大小写与分隔符差异
- 项目 id 由毫秒时间戳改为 UUID，避免碰撞
- 删除项目 / 重置图标时自动清理不再被引用的图标文件
- 添加 / 保存项目时检测重复路径并提示
- 文件监视器监听 `*.json`，手动编辑 `folder-config.json` 也能刷新
- 图标文件缺失时回退默认图标，不再重复弹错误

### 变更

- 添加 / 保存项目时「是否添加到文件夹」「是否设置图标」默认跳过，可一路回车
- 添加项目改为先选文件夹再命名，项目名默认使用文件夹名称
- 整理 manifest：移除冗余的 `activationEvents`、补充 `capabilities` 声明
- 统一包管理器为 pnpm，提交 lock 文件
- 类型定义文件 `project.d.ts` 改为 `project.ts`
